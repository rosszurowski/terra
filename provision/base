
# Updating system...
apt-get update -y -q
apt-get upgrade -y -q
apt-get install build-essential git-core curl -y -q
apt-get install fail2ban unattended-upgrades sudo -y -q

# Disabling root login...
sed -i 's/PermitRootLogin yes/PermitRootLogin no/g' /etc/ssh/sshd_config
service ssh restart